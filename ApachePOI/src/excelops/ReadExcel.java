package excelops;

import java.io.FileInputStream;
import java.io.IOException;

import org.apache.poi.xssf.usermodel.*;

public class ReadExcel {

	public static void main(String[] args) throws IOException {
		String file = ".\\excelfiles\\Undefined PIPS Exceptions_SI - 5th Dec.xlsx";

		/*
		 * FileInputStream stream = new FileInputStream(file); XSSFWorkbook workbook =
		 * new XSSFWorkbook(stream); XSSFSheet sheet = workbook.getSheet("Sheet1");
		 * for(int i=0;i<sheet.getLastRowNum();i++) { for(int
		 * i=0;i<sheet.getColumnOutlineLevel(i)) }
		 */
	}

}
